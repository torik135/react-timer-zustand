import { FlexProps, ButtonProps, StackProps } from "@chakra-ui/react"

export const TimerFlexStyles: FlexProps = {
  paddingBottom: "2rem",
  paddingTop: "2rem",
}

export const BtnStyles: ButtonProps = {
  variant: "solid",
}

export const HStackStyles: StackProps = {
  gap: "1rem",
}
